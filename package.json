{
  "name": "pp-nav",
  "version": "0.0.1",
  "scripts": {
    "build": "weweb build --name=pp-nav --type=section",
    "serve": "weweb serve --type=section"
  },
  "devDependencies": {
    "@weweb/cli": "latest"
  }
}
